import 'package:flutter/material.dart';

class AnnunciPreferitiDummy extends StatelessWidget {
  const AnnunciPreferitiDummy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(
        Icons.bookmark,
        size: 40,
      ),
    );
  }
}
